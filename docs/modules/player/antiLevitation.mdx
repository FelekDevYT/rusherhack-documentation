# AntiLevitation

Removes the levitation effect.

## Settings

- **Mode** (Enum) — *Action taken when levitation is active:*
    - `Remove` — Instantly removes the effect.
    - `Fly` — Allows you to fly while the effect is active.

## Commands

- `antilevitation`
- `antilevitation <setting> [value]`