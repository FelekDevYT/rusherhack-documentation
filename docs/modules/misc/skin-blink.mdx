# SkinBlink

Flash skin layers on and off.

## Settings

- **Mode** (Enum):
    - `All` — Toggle all layers at once.
    - `Random` — Toggle skin layers randomly.
- **Delay** — The speed at which skin layers toggle.

## Commands

- `skinblink`
- `skinblink <setting> [value]`